import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-firestore-crud-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
